///

if(keyboard_check(getkeybind_move_left())||keyboard_check(getkeybind_move_right())||keyboard_check(getkeybind_move_up())||keyboard_check(getkeybind_move_down())){
return true;
}
else{
return false;
}
